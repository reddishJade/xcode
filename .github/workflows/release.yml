name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  verify:
    name: verify release tag
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Checkout release tag
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Install uv and Python
        uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b # v8.1.0
        with:
          version: "0.11.26"
          python-version: "3.12"
          enable-cache: true

      - name: Verify tag matches project version
        shell: bash
        run: |
          project_version="$(uv run --no-project python - <<'PY'
          import tomllib
          from pathlib import Path
          print(tomllib.loads(Path("pyproject.toml").read_text(encoding="utf-8"))["project"]["version"])
          PY
          )"
          test "${GITHUB_REF_NAME}" = "v${project_version}"

      - name: Install project and development dependencies
        run: uv sync --extra dev

      - name: Check formatting
        run: uv run ruff format --check src/

      - name: Lint
        run: uv run ruff check src/

      - name: Type check
        run: uv run pyright src/

      - name: Compile
        run: uv run python -m compileall -q src/

      - name: Test
        run: uv run pytest src/xcode/tests/ -q --tb=short

      - name: Run offline regression suite
        run: >-
          uv run python -m xcode.evals.cli
          --suite regression
          --output-dir .local/eval_runs/release

      - name: Upload regression reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: release-regression-reports
          path: .local/eval_runs/release/
          if-no-files-found: warn
          retention-days: 30

      - name: Minimize uv cache
        if: always()
        run: uv cache prune --ci

  build:
    name: build release artifacts
    needs: verify
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout release tag
        uses: actions/checkout@v7

      - name: Install uv and Python
        uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b # v8.1.0
        with:
          version: "0.11.26"
          python-version: "3.12"
          enable-cache: true

      - name: Build source distribution and wheel
        run: uv build

      - name: Validate package metadata
        run: uvx twine check dist/*

      - name: Upload release artifacts
        uses: actions/upload-artifact@v4
        with:
          name: release-dist
          path: dist/
          if-no-files-found: error
          retention-days: 30

      - name: Minimize uv cache
        if: always()
        run: uv cache prune --ci

  publish-github-release:
    name: publish GitHub Release
    needs: build
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Download release artifacts
        uses: actions/download-artifact@v4
        with:
          name: release-dist
          path: dist/

      - name: Create or update GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ github.ref_name }}
        shell: bash
        run: |
          if gh release view "${TAG}" >/dev/null 2>&1; then
            gh release upload "${TAG}" dist/* --clobber
          else
            gh release create "${TAG}" dist/* --generate-notes --title "${TAG}"
          fi
